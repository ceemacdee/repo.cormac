workflows:
  ios_build:
    name: "iOS Build"
    environment:
      xcode: latest
    scripts:
      - name: Build iOS app
        script: |
          set -o pipefail
          xcodebuild -scheme MonkeyPopper -sdk iphoneos -configuration Release clean build
    artifacts:
      - build/ios/ipa/*.ipa
